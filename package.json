{
  "name": "property-demo",
  "version": "1.0.0",
  "description": "Full-stack property listing demo with React + Node.js",
  "scripts": {
    "install:all": "npm install --prefix server && npm install --prefix client",
    "start:server": "npm start --prefix server",
    "start:client": "npm start --prefix client",
    "dev": "./node_modules/.bin/concurrently \"npm run start:server\" \"npm run start:client\"",
    "setup": "npm run install:all && echo \"\\n✅ Setup complete! Run: npm run dev\""
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
